// require all model files, then include in exported obj
const User = require('./User');
const Metric = require('./Metric');
const Record = require('./Record');

module.exports = { User, Metric, Record };